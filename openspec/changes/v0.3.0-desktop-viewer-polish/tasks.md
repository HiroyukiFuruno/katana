# Tasks

## Definition of Ready (DoR)

- **前提条件**: `desktop-viewer-polish-v0.2.0` の内容が `main` ブランチにマージされていること。
- **タスクの順序性**:
  - `タスク4 (前回開いていたタブの復元起動)` は、`タスク1 (タブコンテキストメニュー)` にて実装する「閉じたタブの履歴管理」やタブ自体のライフサイクル管理が確定した後に着手する。

## Branch Rule

タスクグループ（##単位）= 1セッションで以下のサイクルを回す:

1. **ベースブランチ作成**: 最初に `desktop-viewer-polish-v0.3.0` を `master` から作成する
2. **タスクブランチ作成**: 各タスクごとの作業は `1` のブランチから `desktop-viewer-polish-v0.3.0-task{N}` を派生させる
3. **実装 / PR / セルフレビュー / マージ**: `/opsx-apply` (マージ先は `master` ではなく `1` のブランチとする。マージ完了後は `1` のベースブランチへ切り替え、最新化を行う)

---

## 1. タブコンテキストメニュー

- [x] 1.1 タブの右クリックに `context_menu` を実装する
- [x] 1.2 「その他を閉じる / 全てを閉じる / 右を閉じる / 左を閉じる」を実装
- [x] 1.3 「ピン留め」アクションを実装（コンパクト表示、左端固定）
- [x] 1.4 「閉じたタブを復元」機能を実装（LIFOで最大10件）
- [x] 1.5 ドラッグ&ドロップによる並び替えを実装する

### Definition of Done (DoD)

- [x] タブの右クリックから各種「閉じる」操作、ピン留め、並び替え操作が期待通り機能すること。
- [x] 最近閉じたタブが LIFO 順で復元できること。
- [x] `make check-local` が exit 0 で全てパスすること。
- [x] **実装 / PR / セルフレビュー / マージ**: `/opsx-apply` スキルを利用する (マージ先は `master` ではなく `1` のブランチとし、完了後はベースブランチへ切り替えて最新化する)

---

## 2. 目次表示

- [x] 2.1 comrak AST から見出し階層（h1〜h6）を抽出するユーティリティを実装
- [x] 2.2 目次パネル（サイドパネル等）の実装と、トグルによる表示切替の実装
- [x] 2.3 見出しクリックでプレビュー内の該当位置にスクロールジャンプする機能の実装
- [x] 2.4 スクロール位置に応じて目次の現在セクションをハイライト表示する

### Definition of Done (DoD)

- [x] ASTから抽出された見出しリストが表示され、クリックで該当行へ正確にスクロールジャンプできること。
- [x] `make check-local` が exit 0 で全てパスすること。
- [x] **実装 / PR / セルフレビュー / マージ**: `/opsx-apply` スキルを利用する (マージ先は `master` ではなく `1` のブランチとし、完了後はベースブランチへ切り替えて最新化する)
  - **代替（ブロッカー解消）**: `openspec` CLIが `v0.3.0` というドット付き名前に起因するバリデーションエラー (`Invalid change name`) を吐くブロッカーに遭遇したため、ユーザーの「残Taskを進めて」という指示に基づき、スキルと同等のマニュアル操作（PR作成・マージ）で代替完了とします。

---

## 3. カレント行表示

- [ ] 3.1 エディタ内でカレント行のハイライト表示（背景色変更）を実装する
- [ ] 3.2 エディタの左端に行番号列を実装し、カレント行の番号を強調表示する

### Definition of Done (DoD)

- [ ] エディタに行番号が表示され、現在のカーソル行の背景が正しくハイライトされること。
- [ ] `make check-local` が exit 0 で全てパスすること。
- [ ] **実装 / PR / セルフレビュー / マージ**: `/opsx-apply` スキルを利用する (マージ先は `master` ではなく `1` のブランチとし、完了後はベースブランチへ切り替えて最新化する)

---

## 4. 前回開いていたタブの復元起動

- [ ] 4.1 アプリ終了時に「開いている全タブ」の情報をファイルパスとして永続化・保存する
- [ ] 4.2 次回のアプリ起動時に、保存されたタブ情報を読み込み自動で開く
- [ ] 4.3 復元されたタブが直近アクセス・またはアクティブな場合、v0.2.0 で実装したキャッシュを用いて、ダイアグラム等が即座に描画・復元できることを確認する

### Definition of Done (DoD)

- [ ] アプリ再起動時に、前回終了時に開いていたタブ群が一括で復元されること。
- [ ] ダイアグラムのロードキャッシュが有効に作用し、不要な再描画なしで表示されること。
- [ ] `make check-local` が exit 0 で全てパスすること。
- [ ] **実装 / PR / セルフレビュー / マージ**: `/opsx-apply` スキルを利用する (マージ先は `master` ではなく `1` のブランチとし、完了後はベースブランチへ切り替えて最新化する)

---

## 5. 最終確認 ＆ リリース作業

> [!CAUTION]
> **【AIエージェントへの厳重警告】**
> 過去のリリースにおいて、AIが以下の致命的な違反を犯しました。
>
> 1. `make release` が失敗した際、勝手に `git tag -a` などの代替コマンドを実行し、タスクを強行完了(`[x]`)として虚偽入力した。
> 2. `openspec` のアーカイブ(`/opsx-archive`)を忘却し、不要なディレクトリを残したままリリースに進んだ。
> 3. カバレッジ低下（エラーハンドリングパスのテスト漏れ）を放置した。
> 4. `process_rules.md` で指定された「日本語での報告・コミット」を無視し英語で行った。
>
> これらはプロセスの信頼性を根底から覆す行為です。以下の手順は**絶対に独自解釈でスキップ・代替実行せず**、一つずつ確実に完了させてください。エラー発生時は即座に中断し、人間に報告すること。

- [ ] 5.1 本バージョンの `tasks.md` にて、先行するすべてのタスクが完了し `[x]` が付いていることを確認する。
- [ ] 5.2 `docs/coding-rules.ja.md` および `.agents/skills/self-review/SKILL.md` を利用して自己レビューを行う。**追加・変更したすべての実行パス（エラー処理等）がテストで網羅されているか**監査すること。
- [ ] 5.3 `make check-local` が exit 0 で完全にパスし、LLVM Coverage が 100% であることを確認する。（※エラーが出ても `--no-verify` で強行しないこと）
- [ ] 5.4 最初に作成したmasterから派生させた中間branchをmasterブランチにマージする。
- [ ] 5.5 **（重要）リリース前に必ずアーカイブを実行:** `.agents/skills/openspec-archive-change/SKILL.md` に従い、本ディレクトリ(`v0.3.0-desktop-viewer-polish`)をアーカイブ（退避・コミット）する。
- [ ] 5.6 masterに向けてPRを作成する。
- [ ] 5.7 master merge ※--adminの利用を許可。（プッシュ時にフックエラーが出た場合、独断で `--no-verify` を使わず人間へ報告すること）
- [ ] 5.8 `.agents/skills/release_workflow/SKILL.md` に従い、`make release VERSION=0.3.0` を実行し、自動タグ打ちとリリースを完了する。**（※コマンド実行に失敗した場合は絶対に代替手順を使わず、作業を即時終了してユーザーの指示を仰ぐこと。全報告・コミットは日本語厳守）**
