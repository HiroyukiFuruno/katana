# v0.6.2 Tasks

## 1. 数式レンダリング (KaTeX対応)

- [x] 1.1 Rust用KaTeX/MathJaxライブラリ調査・選定（`mathjax_svg` で決定。V8のビルドオーバーヘッドを許容して妥協なきSVG化）
- [x] 1.2 `egui_commonmark` に数式レンダリング統合 (`MathJaxCache` 実装)
- [x] 1.3 ブロック数式・インライン数式・1行数式のSVG描画対応
- [x] 1.4 fixture で目視確認（スペースを許容する柔軟な事前パース緩和ロジック付き）

## 2. テストフィクスチャ基盤整備

- [x] 2.1 `tests/fixtures/` の復旧（symlink or build script copy）
- [x] 2.2 `sample.md` 結合fixture の再生成 or テストパス修正
- [x] 2.3 全27テストの復旧確認

## 3. 描画品質改善

- [x] 3.1 インラインコード背景の上下中央補正
- [x] 3.2 `<u>` 下線の描画品質確認
- [x] 3.3 `<mark>` ハイライトの描画品質確認

## 4. アコーディオン描画

- [x] 4.1 `<details>/<summary>` の折りたたみUI対応
- [x] 4.2 開閉アニメーション・スタイリング
- [x] 4.3 下に余白を付与5~10px
- [x] 4.4 平開を表現するアイコン、▼、▶の表示位置が文字に対して中央とする。
- [x] 4.5 開いた際のレイアウトには、左の垂直線は含めない

## 5. 注釈

- [x] 5.1 注釈を文末に表示する
- [x] 5.2 注釈の内部リンクを有効にする
- [x] 5.3 注釈の内部リンクをクリックしたら、その注釈の場所までスクロールする
- [x] 5.4 注釈側の末尾にリターンアイコンを追加し、リンクとする注釈側に戻る
- [x] 5.5 注釈にジャンプした際に、複数ある注釈のどこにフォーカスしているのかわかるように注釈自体をborderで囲むような見た目にする。但し他の移動したり、リターンしたらこのborderは消えるものとする。
- [x] 5.6 複数注釈が存在する場合の、配置不正。横並び -> 縦並びとすること
- [x] 5.7 注釈基からジャンプして、戻るを繰り返していると、複数の注釈が、5.5の状態になる。これを防ぐ
- [x] 5.8 戻る際のアイコンをリンクだと分かりやすい、青を基調にした色に変更してください。
- [x] 5.9 注釈の文字サイズを標準より小さくすること
- [x] 5.10 複数の注釈間の間隔を狭くすること
- [x] 5.11 注釈の文字とリンクを上下中央に配置すること

## 6. タスクリスト

- [x] 6.1 表示不正の改善、`- [ ]` 、`- [x]`、の表現はチェックボックスのみとする、リスト固有の記号は使用しない
- [x] 6.2 `- [x]`の[]内にx以外の値が入っている場合はそのまま表示する。
- [x] 6.3 プレビュー側でチェックをクリックしたらコード側に[x]を反映させる。外した場合は[ ]とする
- [x] 6.4 openspecの仕様に対応し、右クリックでチェックボックスにコンテキストメニューを表示し、メニューに[未実施、実施中、完了]の3つの選択肢を表示する。選択した状態をコード側に反映させる。それぞれ[ ]、[/]、[x]とする。
- [x] 6.5 コンテキストメニューの開く範囲をcheckbox + 文字となるように変更する
- [x] 6.6 レイアウト調整、チェックボックスと文字とを上下中央寄せにする。チェックボックスから文字が始まるまでにmerginを設ける。

## 7. 分割モードの改善

- [x] 7.1 縦スクロールの追従性の改善、見出しレベルで追従するようにすることでスクロールの同期ズレを改善する
- [x] 7.2 目次表示時のスクロール位置の同期も同様とする。7.1の実現で自動的に反映される可能性大。
- [x] 7.3 アクティブな行をコード側と同期した際に、プレビュー側でアクティブな行をハイライト表示する
- [x] 7.4 7.3の機構を利用して、プレビューの場合もマウスのポインターがコードの行に重なった際に、コード側をハイライト表示する
- [x] 7.5 プレビューペインのマウスホバー/スクロール同期時（分割モード）における `byte index is not a char boundary` 例外の完全解消
- [x] 7.6 同期ハイライト時に生じるマルチバイト文字（日本語等）の「バイト数/文字数」差分による行ズレ（約7行分の乖離）の完全解消
- [x] 7.7 プレビューペイン上でマウスホバーした際、対象となるMarkdownブロックの背景をハイライトし、コード側との同期状態を視覚的に明示する
- [x] 7.8 コードエディタとプレビュー画面のそれぞれで、マウスクリックにより互いのスクロール位置を完全に同期（自動追従）させる
