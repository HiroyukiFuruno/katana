# 1. ワークスペース・ファイルフィルターUIの追加

- [x] 1.1 `shell_ui.rs` 側の状態管理構造体に、フィルター入力文字列（String）と、フィルター有効トグル（bool）用フィールドを追加する
- [x] 1.2 ワークスペースサイドバーの上部（「更新」ボタンの隣）にフィルター機能ON/OFFのトグルボタンを追加する
- [x] 1.3 トグルが有効な場合、その直下に `egui::TextEdit` による正規表現入力フィールドを表示し、入力時の正規表現コンパイルエラーがあれば背景色を赤くするなどのハンドリングを行う

# 2. ファイルフィルター処理の統合

- [x] 2.1 入力された有効な正規表現（または保持された判定キャッシュ）を引数として、ツリー描画用関数・メソッドに引き回す
- [x] 2.2 ファイルエントリを描画する際、ワークスペースからの相対パスに対し正規表現マッチを実行し、マッチしないファイルはUIツリー要素を描画しないスキップ処理を追加する
- [x] 2.3 上記のフィルタリング結果として配下に描画すべきファイルが1つも存在しなくなったディレクトリ自体を描画しない処理を追加する
- [x] 2.4 これによって毎フレームのパフォーマンスが大きく劣化しないか、動作確認（必要に応じてリストキャッシュ化）を行う

# 3. ファイル検索モーダルの追加

- [x] 3.1 検索モーダル用の状態管理構造体（表示状態 bool, 検索クエリ String, 候補リスト）を追加する
- [x] 3.2 検索機能を呼び出すためのメニューバー配置、およびOSの標準的なショートカットキー（macOS: Cmd+P 等）ハンドリングを追加する
- [x] 3.3 `egui::Window` （あるいは中央オーバーレイ）を用いて、検索ワード入力フィールドと検索結果リストを表示するモーダルUIを作成する

# 4. 検索結果の表示とファイルオープン

- [x] 4.1 検索ワードが変更されるたびに、ワークスペースの保持する全ファイルリストから相対パス部分一致判定を行い、上限（例: 100件）までの該当リストを抽出・キャッシュするロジックを実装する
- [x] 4.2 検索結果にヒットしたパスをモーダル内に一覧（ボタン等）表示し、スクロール領域でラップする
- [x] 4.3 結果アイテムがクリックされたとき、該当ファイルが存在するか `std::path::Path::exists` 等で再確認した上で「新しいタブを開く」処理をトリガーし、成功時にモーダルを閉じる

# 5. 最終確認 ＆ リリース作業

- [x] 5.1 自己レビュー・修正
- [x] 5.2 make checkがexit 0
- [x] 5.3 feature/v0.1.6ブランチをmasterブランチにマージする
- [x] 5.4 masterに向けてPRを作成する
- [x] 5.5 master merger ※--adminの利用を許可
- [x] 5.6 v0.1.6でタグをうち、リリースymlを実行
