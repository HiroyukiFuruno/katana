# lefthook.yml
pre-commit:
  parallel: true
  commands:
    formatting:
      glob: "*.rs"
      run: make fmt
    clippy:
      glob: "*.rs"
      run: make lint-fix

pre-push:
  commands:
    ci:
      run: make ci
