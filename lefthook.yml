# lefthook.yml
pre-commit:
  parallel: true
  commands:
    formatting:
      glob: "*.rs"
      run: make fmt
    clippy:
      glob: "*.rs"
      run: make lint-fix

pre-push:
  commands:
    ci:
      glob: "*.{rs,toml,lock}"
      run: make ci
